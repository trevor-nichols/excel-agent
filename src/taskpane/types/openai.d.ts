/**
 * Minimal module declaration to satisfy the compiler/linter when the OpenAI
 * types are not picked up by the toolchain. The actual types come from the
 * installed SDK at runtime.
 */
declare module "openai";

